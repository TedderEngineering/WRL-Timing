{
  "buildCommand": "cd client && npm ci && npx vite build",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'skip'",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://wrl-timing-production.up.railway.app/api/:path*"
    }
  ]
}
